"""Datasets and tokenizers.

Planned: character-level tokenizer + TinyShakespeare first (zero dependencies,
trains on CPU), then our own from-scratch BPE (Sennrich 2015, as used by GPT-2),
then TinyStories-scale data for the home GPU.
"""
